// Packages imports
import { ParsedQs } from "qs";
import { ObjectId } from "mongoose";
import Redis from "ioredis";

// utils imports
import { APIFeatures, AppError } from "@utils/index";

export class ReadingListCRUDService {
  /**
   * Retrieves all reading list items.
   */
  public getAllReadingListItems() {}

  /**
   * Retrieves a specific reading list item.
   */
  public getReadingListItem() {}

  /**
   * Creates a new reading list item.
   */
  public createReadingListItem() {}

  /**
   * Updates an existing reading list item.
   */
  public updateReadingListItem() {}

  /**
   * Deletes a reading list item.
   */
  public deleteReadingListItem() {}
}
