// interface import
import { IAuthRepository } from "../interfaces/authRepository.interface";

export class AuthRepository implements IAuthRepository {}

//! The repository class located on the users feature under the repository folder and will be moved to here later.
